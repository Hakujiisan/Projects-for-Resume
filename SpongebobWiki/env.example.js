// env.example.js — copy to env.js and fill values for your environment
// Do NOT commit env.js with real secrets. Keep env.js out of version control.

window.__ENV__ = window.__ENV__ || {};

// Tenor (Google) API key used for fetching GIFs
// Replace the placeholder below with your real key, then create env.js from this file.
window.__ENV__.TENOR_API_KEY = 'REPLACE_WITH_YOUR_TENOR_API_KEY';

