// env.js — local environment overrides (do not commit real keys)
// Create this file from env.example.js and add it to .gitignore

window.__ENV__ = window.__ENV__ || {};

// Example: window.__ENV__.TENOR_API_KEY = 'AIza...';

